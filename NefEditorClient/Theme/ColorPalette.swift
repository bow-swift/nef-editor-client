import SwiftUI

extension Color {
    static var nef: Color {
        Color("nefColor")
    }
    
    static var card: Color {
        Color("card")
    }
    
    static var shadow: Color {
        Color("shadow")
    }
}
